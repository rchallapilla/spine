# Supabase (required)
# Project Settings -> API Keys. Accepts either the new sb_publishable_.../sb_secret_...
# keys or the legacy anon/service_role JWT keys -- both formats work unchanged.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-publishable-or-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-secret-or-service-role-key

# Server-side overrides (optional; fall back to the NEXT_PUBLIC values)
# SUPABASE_URL= 
# SUPABASE_ANON_KEY=

# Auth allowlist (comma-separated emails)
ALLOWED_EMAILS=

# Anthropic (required for M5+ unless OPENROUTER_API_KEY is set)
ANTHROPIC_API_KEY=

# OpenRouter (optional). When set, all LLM calls route through OpenRouter's
# Anthropic-compatible endpoint instead of Anthropic directly, and
# ANTHROPIC_API_KEY is no longer needed. Get a key at https://openrouter.ai/keys
# OPENROUTER_API_KEY=

# Vercel Cron protection (required for M6+)
CRON_SECRET=generate-a-long-random-string

# Web Push VAPID keys (required for M7+)
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:you@example.com

# Site URL for auth redirects (optional; derived from the request when unset)
# NEXT_PUBLIC_SITE_URL=https://your-app.vercel.app
